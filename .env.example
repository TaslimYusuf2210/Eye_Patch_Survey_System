# API base URL (Vercel production)
# REQUIRED for production deployments. Set this to your deployed backend URL,
# e.g. https://your-api.example.com
#
# If unset, the app falls back to `http://<current-hostname>:3000`, which only
# works on localhost / your LAN, NOT on Vercel.
VITE_BASE_URL=
