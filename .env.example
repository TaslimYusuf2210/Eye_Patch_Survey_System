# API base URL — the deployed backend (Heroku).
# The app defaults to https://survey-system-backend-ec56827922fa.herokuapp.com, so
# this only needs to be set if you want to point at a different backend.
VITE_BASE_URL=https://survey-system-backend-ec56827922fa.herokuapp.com
